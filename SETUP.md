# 人生笔记 - 配置指南

## 1. 创建飞书多维表格

### 1.1 创建多维表格

1. 打开 [飞书](https://feishu.cn)，点击左上角「新建」→「多维表格」
2. 将表格命名为「人生笔记」或你喜欢的名字
3. 创建以下数据表：

#### 表1：心情感悟表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 时间 | 日期时间 | 记录时间 |
| 内容 | 文本 | 感悟内容 |
| 具体事件 | 文本 | 具体发生了什么事 |
| 思考 | 文本 | 当时的思考和分析 |
| 情绪标签 | 多选 | 开心、焦虑、平静、疲惫、困惑、感恩、释然、委屈、愤怒、期待... |
| 场景标签 | 多选 | 人际、工作、生活、成长、关系、独处、家庭... |
| 下次怎么做 | 文本 | 经验总结和改进方向 |

#### 表2：纪念日表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 纪念日名称 | 文本 | 纪念日名称 |
| 日期 | 日期 | 纪念日日期 |
| 类型 | 单选 | 生日、节日、入职纪念日、其他 |
| 提醒状态 | 文本 | 已提醒/未提醒 |

#### 表3：礼物记录表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 礼物内容 | 文本 | 礼物名称 |
| 对象 | 文本 | 送/收对象 |
| 场合 | 文本 | 生日、节日、日常... |
| 日期 | 日期 | 记录日期 |
| 年份 | 数字 | 年份 |
| 花费 | 数字 | 花费金额 |
| 备注 | 文本 | 备注说明 |
| 对方反馈 | 文本 | 对方反馈 |
| 纪念日 | 关联 | 关联到纪念日表 |

---

## 2. 获取 Base Token

### 2.1 通过飞书开放平台获取

1. 访问 [飞书开放平台](https://open.feishu.cn/)
2. 创建企业自建应用
3. 在「凭证与基础信息」中获取 `App ID` 和 `App Secret`
4. 调用接口获取 `user_access_token` 或 `tenant_access_token`

### 2.2 通过 lark-cli 获取

```bash
# 登录飞书
lark-cli auth login

# 获取 token 信息
lark-cli auth status
```

---

## 3. 获取 Table ID

### 3.1 通过多维表格 URL

打开你的多维表格，URL 格式如下：

```
https://xxx.feishu.cn/base/[BASE_TOKEN]?table=[TABLE_ID]
```

- `BASE_TOKEN`：多维表格的唯一标识
- `TABLE_ID`：每个数据表的唯一标识

### 3.2 通过 lark-cli 获取

```bash
# 列出所有表
lark-cli base +table-list --base-token "YOUR_BASE_TOKEN"
```

---

## 4. 配置脚本

### 4.1 下载脚本

将 `scripts/` 目录下的所有脚本下载到本地。

### 4.2 替换占位符

编辑每个脚本，替换以下占位符：

| 占位符 | 说明 | 获取方式 |
|--------|------|----------|
| `YOUR_BASE_TOKEN` | 多维表格 Token | 见上方「获取 Base Token」 |
| `YOUR_BASE_URL` | 飞书多维表格地址 | 如 `https://xxx.feishu.cn/base/YOUR_BASE_TOKEN` |
| `YOUR_FEELING_TABLE_ID` | 心情感悟表 ID | 在多维表格中打开「心情感悟表」 |
| `YOUR_ANNIVERSARY_TABLE_ID` | 纪念日表 ID | 在多维表格中打开「纪念日表」 |
| `YOUR_GIFT_TABLE_ID` | 礼物记录表 ID | 在多维表格中打开「礼物记录表」 |

### 4.3 脚本使用示例

```bash
# 给脚本添加执行权限
chmod +x scripts/*.sh

# 记录人生笔记
./scripts/record_feeling.sh "和同事吵架了" "觉得自己太冲动" "后悔" "人际" "下次先冷静10分钟"

# 添加纪念日
./scripts/create_anniversary.sh "家人生日" "2026-04-20"

# 记录礼物
./scripts/record_gift.sh "鲜花" "2026" "150" "生日礼物" "很喜欢"

# 查看笔记
./scripts/query_feeling.sh 7

# 周数据
./scripts/query_week_feeling.sh
```

---

## 5. 设置提醒

### 5.1 创建飞书日历日程

为了实现纪念日提醒功能，需要在飞书日历中创建日程事件：

1. 打开飞书日历
2. 创建三个日程事件（提前7天、提前1天、当天）
3. 设置提醒时间

### 5.2 配置定时任务

可以使用系统定时任务（如 cron）自动执行脚本：

```bash
# 每天晚上11点检查纪念日
0 23 * * * /path/to/scripts/check_anniversaries.sh
```

---

## 6. 注意事项

1. **情绪标签**：心情感悟的情绪标签是**多选**，可以同时有多种情绪
2. **场景标签**：场景标签也是**多选**
3. **纪念日提醒**：需要创建3个日程事件（提前7天、提前1天、当天）
4. **礼物记录**：礼物记录不单独提醒，在纪念日提醒时汇总展示
5. **数据安全**：请勿将你的 Token 上传到公开仓库
